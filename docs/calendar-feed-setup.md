# Calendar Feed Setup

World Cup Calendar live feeds are iCalendar URLs from `/api/calendar/feed`.

Examples:

```text
/api/calendar/feed?mode=all
/api/calendar/feed?mode=teams&teams=Canada,Portugal
/api/calendar/feed?mode=cities&cities=Toronto,Vancouver
```

Feeds appear as subscribed calendars. Refresh timing depends on each calendar provider.
